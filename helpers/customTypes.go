package helpers

/* RequestIDKey Defines a type to use as a user model key in a request context */
type RequestUserKey struct{}

/* RequestQueryIdKey Defines a type to use as a query id key in a request context */
type RequestQueryIdKey struct{}

/* RequestPageKey Defines a type to use as a page param key in a request context */
type RequestPageKey struct{}

/* RequestQueryIdKey Defines a type to use as a limit param key in a request context */
type RequestLimitKey struct{}
